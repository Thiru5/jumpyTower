services:
  - type: static
    name: valentine-tower
    env: production
    # No build step needed for this plain HTML/JS app. Leave empty so Render simply serves files.
    buildCommand: ""
    # Publish the project root (index.html is at repo root)
    publishPath: "/"
